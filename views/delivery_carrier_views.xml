<odoo>
    <record id="view_delivery_carrier_form_postnl" model="ir.ui.view">
        <field name="name">delivery.carrier.form.postnl</field>
        <field name="model">delivery.carrier</field>
        <field name="inherit_id" ref="delivery.view_delivery_carrier_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='delivery_type']" position="after">
                <!-- Odoo 18+: use boolean invisible (no attrs/states) and avoid <label> without 'for' -->
                <group invisible="delivery_type != 'postnl'">
                    <field name="postnl_wbs_rule_id"/>
                    <div class="o_form_label">PostNL carrier uses global settings under PostNL → Settings.</div>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
